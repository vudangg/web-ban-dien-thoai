<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Chỉnh sửa danh mục</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <div class="container mx-auto py-8">
    <h1 class="text-2xl font-bold mb-6">✏️ Chỉnh sửa danh mục</h1>
    <a href="/admin/categories" class="text-blue-500 hover:underline mb-4 inline-block">← Quay lại</a>

    <form action="/admin/categories/edit/<%= category._id %>" method="POST" class="space-y-4 bg-white p-6 rounded shadow">
      <div>
        <label class="block text-gray-700">Tên danh mục</label>
        <input name="name" value="<%= category.name %>" required
               class="w-full px-4 py-2 border rounded mt-1" />
      </div>
      <div>
        <label class="block text-gray-700">Mô tả</label>
        <input name="description" value="<%= category.description %>"
               class="w-full px-4 py-2 border rounded mt-1" />
      </div>
      <button type="submit"
              class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700">
        Lưu thay đổi
      </button>
    </form>
  </div>
</body>
</html>
