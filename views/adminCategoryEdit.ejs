<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Chỉnh sửa danh mục</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">

  <div class="container mx-auto py-8 flex-grow">
    <h1 class="text-2xl font-bold mb-6">Chỉnh sửa danh mục</h1>
    <a href="/admin/categories" class="text-blue-500 hover:underline mb-4 inline-block">← Quay lại</a>

    <form action="/admin/categories/edit/<%= category._id %>" method="POST" class="space-y-6 bg-white p-8 rounded-lg shadow-lg">
      <div>
        <label class="block text-gray-700 font-semibold">Tên danh mục</label>
        <input name="name" value="<%= category.name %>" required
               class="w-full px-4 py-2 border rounded-lg mt-2 focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>
      <div>
        <label class="block text-gray-700 font-semibold">Mô tả</label>
        <input name="description" value="<%= category.description %>"
               class="w-full px-4 py-2 border rounded-lg mt-2 focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>
      <button type="submit"
              class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500">
        Lưu thay đổi
      </button>
    </form>
  </div>

  <!-- Footer -->
  <footer class="bg-gray-800 text-white py-6 mt-12">
    <div class="container mx-auto px-6 text-center">
      <p>&copy; <%= new Date().getFullYear() %> Cửa Hàng Điện Thoại - Bản quyền thuộc về chúng tôi.</p>
      <nav class="mt-4 flex justify-center space-x-6 text-sm">
        <a href="/privacy-policy" class="hover:underline hover:text-white transition">Chính sách bảo mật</a>
        <a href="/terms" class="hover:underline hover:text-white transition">Điều khoản sử dụng</a>
        <a href="/contact" class="hover:underline hover:text-white transition">Liên hệ</a>
      </nav>
    </div>
  </footer>

</body>
</html>
