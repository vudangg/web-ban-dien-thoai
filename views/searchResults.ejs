<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kết quả tìm kiếm</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
  <header class="bg-blue-500 text-white p-4 text-center">
    <h1 class="text-2xl font-bold">Kết quả tìm kiếm cho: "<%= query %>"</h1>
    <nav class="mt-2 space-x-4">
      <a href="/" class="hover:underline">Trang chủ</a>
      <a href="/products" class="hover:underline">Sản phẩm</a>
      <a href="/cart" class="hover:underline">Giỏ hàng</a>
    </nav>
  </header>

  <main class="container mx-auto mt-10 p-5 bg-white shadow-lg rounded-lg">
    <% if (products.length > 0) { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
        <% products.forEach(product => { %>
          <div class="bg-gray-50 p-4 rounded shadow">
            <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-48 object-cover rounded">
            <h2 class="mt-2 font-bold text-lg"><%= product.name %></h2>
            <p class="text-red-500 font-bold">
              <%= Number(product.price).toLocaleString('vi-VN') %> VND
            </p>
            <a href="/products/<%= product._id %>" class="block mt-2 text-center bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
              Xem chi tiết
            </a>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p class="text-center text-red-500 text-xl">Không tìm thấy sản phẩm nào phù hợp.</p>
    <% } %>
  </main>
</body>
</html>
