<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thanh toán</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto mt-10 p-5 bg-white shadow-lg rounded-lg">
        <h1 class="text-2xl font-bold text-center">Chọn phương thức thanh toán</h1>
        <% if (cart && cart.length > 0) { %>
            <div class="mt-4 p-4 border rounded-lg bg-gray-50">
                <h2 class="text-lg font-semibold">Sản phẩm trong giỏ hàng</h2>
                <ul class="mt-2 space-y-2">
                    <% cart.forEach(item => { %>
                        <li class="flex justify-between border-b pb-1">
                            <span><%= item.name %> (x<%= item.quantity || 1 %>)</span>
                            <span class="text-red-500">
                                <%= (parseFloat(item.price || 0) * (item.quantity || 1)).toLocaleString() %> VND
                            </span>
                        </li>
                    <% }); %>
                </ul>
                <% let totalPrice = cart.reduce((sum, item) => sum + (parseFloat(item.price || 0) * (item.quantity || 1)), 0); %>
                <div class="mt-4 text-right font-semibold">
                    Tổng cộng: <span class="text-red-500"><%= totalPrice.toLocaleString() %> VND</span>
                </div>
            </div>
        <% } else { %>
            <p class="text-center text-gray-500 mt-4">Giỏ hàng của bạn đang trống.</p>
        <% } %>
        <form action="/checkout" method="POST" class="mt-5">
            <div class="flex flex-col space-y-3">
                <label class="flex items-center space-x-2">
                    <input type="radio" name="paymentMethod" value="momo" required>
                    <span>Thanh toán qua MoMo</span>
                </label>
                <label class="flex items-center space-x-2">
                    <input type="radio" name="paymentMethod" value="bank">
                    <span>Internet Banking</span>
                </label>
                <label class="flex items-center space-x-2">
                    <input type="radio" name="paymentMethod" value="cod">
                    <span>Thanh toán khi nhận hàng (COD)</span>
                </label>
            </div>
            <button type="submit" class="mt-5 w-full bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600">
                Tiến hành thanh toán
            </button>
        </form>
    </div>
</body>
</html>
