<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Quản lý tài khoản</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800 flex flex-col min-h-screen">

  <!-- Header -->
  <header class="bg-red-600 text-white p-6 shadow-md">
    <div class="container mx-auto flex justify-between items-center">
      <img src="/uploads/logo.png" alt="Logo" class="w-12 h-12 object-contain">
      <h1 class="text-2xl font-bold ml-4">Quản lý tài khoản</h1>
      <a href="/admin" class="bg-white text-red-600 px-4 py-2 rounded hover:bg-gray-100">← Quay lại Dashboard</a>
    </div>
  </header>

  <!-- Main Content -->
  <main class="flex-grow container mx-auto p-6">
    <h2 class="text-xl font-semibold mb-4">Danh sách người dùng</h2>
    <table class="min-w-full bg-white rounded-lg shadow-md">
      <thead class="bg-gray-200 text-left">
        <tr>
          <th class="p-3">Tên</th>
          <th class="p-3">Email</th>
          <th class="p-3">Vai trò</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach(user => { %>
          <tr class="border-t hover:bg-gray-50">
            <td class="p-3"><%= user.name %></td>
            <td class="p-3"><%= user.email %></td>
            <td class="p-3 capitalize"><%= user.role || 'user' %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <a href="/admin" class="mt-6 inline-block bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">← Quay lại trang admin</a>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 text-white py-6 mt-12">
    <div class="container mx-auto text-center text-sm">
      <p>&copy; <%= new Date().getFullYear() %> CellphoneS - Tất cả quyền được bảo lưu.</p>
      <nav class="mt-3 flex justify-center space-x-6 text-gray-300">
        <a href="/privacy-policy" class="hover:underline hover:text-white transition">Chính sách bảo mật</a>
        <a href="/terms" class="hover:underline hover:text-white transition">Điều khoản</a>
        <a href="/contact" class="hover:underline hover:text-white transition">Liên hệ</a>
      </nav>
    </div>
  </footer>

</body>
</html>
