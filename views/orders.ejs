<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Quản lý đơn hàng</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">

  <header class="bg-red-600 text-white p-6 shadow-md">
    <div class="container mx-auto flex justify-between items-center">
      <!-- Thêm logo ở đây -->
      <img src="/uploads/logo.png" alt="Logo" class="w-12 h-12 object-contain">
      <h1 class="text-2xl font-bold ml-4">🛒 Quản lý đơn hàng</h1>
      <a href="/admin" class="bg-white text-red-600 px-4 py-2 rounded hover:bg-gray-100">← Về Dashboard</a>
    </div>
  </header>

  <main class="container mx-auto p-6 flex-grow">
    <% if (orders.length) { %>
      <table class="w-full bg-white rounded-lg shadow overflow-hidden">
        <thead class="bg-gray-200 text-left">
          <tr>
            <th class="p-3">Mã đơn</th>
            <th class="p-3">Khách hàng</th>
            <th class="p-3">Tổng tiền</th>
            <th class="p-3">Trạng thái</th>
            <th class="p-3">Ngày tạo</th>
          </tr>
        </thead>
        <tbody>
          <% orders.forEach(o => { %>
            <tr class="border-t hover:bg-gray-50">
              <td class="p-3"><%= o._id %></td>
              <td class="p-3"><%= o.userId %></td>
              <td class="p-3 text-red-600 font-semibold"><%= o.total.toLocaleString('vi-VN') %> VND</td>
              <td class="p-3 capitalize"><%= o.status %></td>
              <td class="p-3"><%= o.createdAt.toLocaleString() %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } else { %>
      <p class="text-center text-gray-500">Chưa có đơn hàng nào.</p>
    <% } %>
  </main>

  <!-- Footer đồng bộ -->
  <footer class="bg-gray-800 text-white py-6 mt-12">
    <div class="container mx-auto text-center text-sm">
      <p>&copy; <%= new Date().getFullYear() %> CellphoneS - Tất cả quyền được bảo lưu.</p>
      <nav class="mt-3 flex justify-center space-x-6 text-gray-300">
        <a href="/privacy-policy" class="hover:underline hover:text-white transition">Chính sách bảo mật</a>
        <a href="/terms" class="hover:underline hover:text-white transition">Điều khoản</a>
        <a href="/contact" class="hover:underline hover:text-white transition">Liên hệ</a>
      </nav>
    </div>
  </footer>

</body>
</html>
