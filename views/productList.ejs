<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gray-50">

  <!-- Header -->
  <header class="bg-red-600 text-white px-6 py-3 flex items-center justify-between">
    <!-- Logo -->
    <div class="flex items-center gap-2">
      <img src="/images/logo.png" alt="Logo" class="w-6 h-6" />
      <span class="font-bold text-lg">cellphoneS</span>
    </div>

    <!-- Thanh tìm kiếm -->
    <form action="/search" method="GET" class="flex-1 mx-6">
      <input
        type="text"
        name="keyword"
        placeholder="Bạn cần tìm gì?"
        class="w-full px-4 py-2 rounded focus:outline-none text-black"
      />
    </form>

    <!-- Các nút bên phải -->
    <div class="flex items-center gap-4">
      <!-- Giỏ hàng -->
      <a href="/cart" class="flex items-center hover:text-yellow-300 transition duration-200">
        <i class="fas fa-shopping-cart mr-1"></i> Giỏ hàng
      </a>

      <!-- Nút Admin -->
      <% if (user && user.role === 'admin') { %>
        <a href="/admin/dashboard" class="bg-red-500 hover:bg-red-700 text-white font-semibold py-1 px-3 rounded">
          <i class="fas fa-user-shield mr-1"></i> Admin
        </a>
      <% } %>

      <!-- Nút đăng xuất -->
      <a href="/logout" class="hover:text-yellow-300 transition duration-200">
        Đăng xuất
      </a>
    </div>
  </header>

  <!-- Danh sách sản phẩm -->
  <main class="container mx-auto mt-10 p-6 bg-white shadow-lg rounded-lg">
    <h2 class="text-xl font-semibold mb-6 text-gray-800">
      📦 Danh sách sản phẩm <%= typeof category !== 'undefined' && category ? `- ${category}` : "" %>
    </h2>

    <!-- Kiểm tra nếu có sản phẩm -->
    <% if (products && products.length > 0) { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
        <% products.forEach(product => { %>
          <div id="product-<%= product._id %>" class="border p-6 rounded-lg shadow-lg bg-white hover:shadow-2xl transition-transform transform hover:scale-105">
            <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-56 object-cover rounded-lg mb-4">

            <h2 class="text-lg font-semibold text-gray-900"><%= product.name %></h2>
            <p class="text-gray-600 mt-2">
              <strong class="font-semibold text-gray-700">Giá:</strong>
              <span class="text-red-600 font-bold">
                <%= Number(product.price).toLocaleString('vi-VN') %> VND
              </span>
            </p>

            <a href="/products/<%= product._id %>" class="block mt-2 text-blue-500 hover:underline">
              Xem chi tiết
            </a>

            <!-- Form thêm vào giỏ hàng -->
            <form action="/cart/add" method="POST" class="mt-4">
              <input type="hidden" name="productId" value="<%= product._id %>">
              <input type="hidden" name="name" value="<%= product.name %>">
              <input type="hidden" name="price" value="<%= product.price %>">
              <button type="submit" class="w-full bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-600 transition duration-200">
                🛒 Thêm vào giỏ hàng
              </button>
            </form>

            <% if (user && user.role === 'admin') { %>
              <div class="mt-4 flex justify-between">
                <button 
                  type="button"
                  class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 text-sm"
                  onclick="deleteProduct('<%= product._id %>');">
                  🗑️ Xoá
                </button>
                <a href="/products/edit/<%= product._id %>" class="text-yellow-500 hover:underline">
                  ✏️ Chỉnh sửa
                </a>
              </div>
            <% } %>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <p class="text-gray-500 text-center">
        ⛔ Chưa có sản phẩm nào. <a href="/products/create" class="text-blue-500 underline">Thêm sản phẩm ngay</a>!
      </p>
    <% } %>

    <!-- Nút thêm sản phẩm & về dashboard -->
    <% if (user && user.role === 'admin') { %>
      <div class="flex justify-end mt-8 space-x-4">
        <a href="/products/create" class="bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-600 transition duration-200">
          ➕ Thêm sản phẩm
        </a>
        <a href="/admin" class="bg-gray-800 text-white px-4 py-2 rounded-lg hover:bg-gray-900 transition duration-200">
          ← Quay về Dashboard
        </a>
      </div>
    <% } %>
  </main>

  <!-- Script xử lý xóa sản phẩm -->
  <script>
    function deleteProduct(productId) {
      if (confirm("Bạn có chắc chắn muốn xoá sản phẩm này?")) {
        fetch(`/products/delete/${productId}`, {
          method: 'DELETE',
          headers: {
            'Content-Type': 'application/json',
          },
        })
        .then(response => response.json())
        .then(data => {
          if (data.message) {
            alert(data.message);
            document.getElementById(`product-${productId}`).remove();
          } else {
            alert("Có lỗi xảy ra khi xóa sản phẩm.");
          }
        })
        .catch(error => {
          console.error('Lỗi:', error);
          alert("Lỗi khi xóa sản phẩm.");
        });
      }
    }
  </script>
</body>
</html>
